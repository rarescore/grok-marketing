import { useState } from 'react'
import SectionIntro from '../components/SectionIntro'
import { siteConfig } from '../data/siteConfig'
export default function Contact({ chapter }) {
  const [status, setStatus] = useState('')
  const submit = (event) => { event.preventDefault(); const data = new FormData(event.currentTarget); const subject = encodeURIComponent(`RS Marketing inquiry — ${data.get('company') || data.get('name')}`); const body = encodeURIComponent(`Name: ${data.get('name')}\nCompany: ${data.get('company')}\nEmail: ${data.get('email')}\nInterested in: ${data.get('service')}\n\n${data.get('message')}`); setStatus('Opening your email client…'); window.location.href = `mailto:${siteConfig.email}?subject=${subject}&body=${body}` }
  return <section id="contact" className="scene-section contact-section" data-chapter="8"><div className="section-shell section-shell--split"><SectionIntro chapter={chapter}><a className="contact-email reveal-copy" href={`mailto:${siteConfig.email}`}>{siteConfig.email}</a></SectionIntro><form className="contact-form reveal-panel" onSubmit={submit}><div className="field"><label htmlFor="name">Your name</label><input id="name" name="name" required autoComplete="name" /></div><div className="field"><label htmlFor="company">Company</label><input id="company" name="company" autoComplete="organization" /></div><div className="field"><label htmlFor="email">Email</label><input id="email" name="email" type="email" required autoComplete="email" /></div><div className="field"><label htmlFor="service">What do you need?</label><select id="service" name="service" defaultValue=""><option value="" disabled>Select an option</option><option>100/100 SEO Website</option><option>Starter package</option><option>Growth package</option><option>Scale package</option><option>Empire package</option><option>Visibility scan</option></select></div><div className="field field--full"><label htmlFor="message">Tell us about the goal</label><textarea id="message" name="message" rows="4" required /></div><button className="button button--solid" type="submit">Send the signal <span>↗</span></button><p className="form-status" aria-live="polite">{status}</p></form></div></section>
}
